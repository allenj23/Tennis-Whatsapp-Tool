node_modules/
.wwebjs_auth/
.wwebjs_cache/
uploads/
*.log

# Local data cache (contacts restored on restart)
data/

# Google service-account key files — never commit these
*service-account*.json
*credentials*.json
*.serviceaccount.json
whatsapp-tools-*.json

# Vendor Google OAuth credentials (never commit)
oauth-spike.credentials.json
vendor-google-oauth.json
src/google-auth/baked-credentials.js
gcs-allowed-users.json

# Release builds
dist/
release/
